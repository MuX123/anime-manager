# ACG 收藏庫 - 設計系統文檔

## 📁 CSS 架構

### 檔案結構

```
css/
├── variables.css    # CSS 變數（色彩、字體、過渡）
├── base.css         # 重置樣式和基礎排版
├── layout.css       # 佈局系統（網格、容器）
├── components.css   # UI 組件（按鈕、卡片、模態框）
├── animations.css   # 動畫效果
└── responsive.css   # 響應式設計
```

---

## 🎨 設計變數

### 色彩系統

```css
:root {
    /* 背景色彩 */
    --bg-dark: #0a0e1a;
    --bg-darker: #050609;
    --panel-bg: rgba(10, 15, 25, 0.95);
    --glass-bg: rgba(15, 20, 35, 0.4);

    /* 霓虹色彩 */
    --neon-blue: #00d4ff;
    --neon-cyan: #00ffff;
    --neon-purple: #b026ff;
    --neon-pink: #ff006e;
    --neon-green: #00ff41;

    /* 文字色彩 */
    --text-main: #e0e6ed;
    --text-secondary: #a8b0c0;

    /* 動畫過渡 */
    --transition-smooth: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    --transition-bounce: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}
```

### 字體系統

- **標題字體**: 'Orbitron', sans-serif
- **內文字體**: 'Space Mono', 'Microsoft JhengHei', monospace
- **中文字體**: Noto Sans TC

---

## 📱 響應式斷點

| 斷點 | 類別 | 網格列數 |
|------|------|----------|
| 1400px | 大型螢幕 | 4 列 |
| 1200px | 大型螢幕 | 4 列 |
| 1024px | 平板 | 3 列 |
| 768px | 手機 | 2 列 |
| 480px | 小螢幕 | 1 列 |

---

## 🧩 組件規範

### 按鈕 (.btn-primary)

```html
<button class="btn-primary">按鈕文字</button>
```

**狀態**:
- Default: 透明背景、藍色邊框
- Hover: 發光效果、放大
- Active: 背景填充

### 卡片 (.anime-card)

```html
<div class="anime-card">
    <!-- 卡片內容 -->
</div>
```

**特性**:
- 玻璃擬態背景
- 左側彩色邊條（根據評分變色）
- Hover 上浮效果
- 進入動畫

### 模態框 (.modal)

```html
<div class="modal active">
    <div class="modal-content">
        <!-- 內容 -->
    </div>
</div>
```

**特性**:
- 模糊背景
- 滑入動畫
- 最大寬度 1100px

---

## 🎬 動畫系統

### 預定義動畫

| 動畫名稱 | 用途 | 持續時間 |
|----------|------|----------|
| `cardSlideIn` | 卡片進入 | 0.4s |
| `modalSlideIn` | 模態框出現 | 0.4s |
| `slideInUp` | 內容區域 | 0.5s |
| `fadeInScale` | 頁面載入 | 0.6s |
| `menuSlideIn` | 選單滑入 | 0.4s |

### 緩動函數

```css
/* 彈跳效果 */
cubic-bezier(0.34, 1.56, 0.64, 1)

/* 平滑過渡 */
cubic-bezier(0.25, 0.46, 0.45, 0.94)
```

---

## 📋 命名規範

### 選擇器命名

1. **組件類**: 使用 kebab-case (如 `.anime-card`)
2. **變體**: 使用連字符 (如 `.force-mobile-layout`)
3. **版本標記**: 使用 v數字 (如 `.detail-container-v35`)
4. **修飾符**: 使用 `--` (如 `.btn-primary--active`)

### 檔案命名

- 小寫 kebab-case
- 模組化結構

---

## 🚀 效能優化

### 最佳實踐

1. **使用 `will-change`** - 預告瀏覽器即將變化的屬性
2. **優先使用 `transform` 和 `opacity`** - 這些屬性不會觸發重繪
3. **避免 `!important`** - 除非絕對必要
4. **使用 CSS 變數** - 提高可維護性和效能

### 動畫效能

```css
/* ✅ 好的動畫 */
.anime-card {
    transform: translateY(-10px);
    opacity: 0.5;
}

/* ❌ 避免的動畫 */
.anime-card {
    width: 100%;
    height: 100%;
    left: 10px;
}
```

---

## 🔧 維護指南

### 新增組件

1. 在 `components.css` 中定義樣式
2. 在 `animations.css` 中添加必要動畫
3. 在 `responsive.css` 中添加響應式規則
4. 更新本文檔

### 修改現有樣式

1. 確認修改不影響其他組件
2. 在相應的模組檔案中修改
3. 測試所有斷點
4. 更新 CHANGELOG

---

## 📊 版本歷史

### v6.1.0 (2026-02-02)

- ✅ CSS 模組化拆分
- ✅ 新增 RWD 斷點 (1024px, 1400px, 480px)
- ✅ 修復動畫衝突
- ✅ 優化載入效能
- ✅ 建立設計系統文檔
